from fastapi import APIRouter

router = APIRouter()

# ── Analytics endpoints will be added in Phase 8 ──────────
# GET /analytics/stats
# GET /analytics/mood-distribution
# GET /analytics/mood-activity
# GET /analytics/insight
